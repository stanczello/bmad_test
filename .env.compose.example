# Copy to .env.compose and adjust as needed.

# Global runtime mode for containerized app services.
APP_ENV=production

# Host ports for production-like services.
BACKEND_PORT=3000
FRONTEND_PORT=4173

# Host port for dev profile backend service.
BACKEND_DEV_PORT=3001
